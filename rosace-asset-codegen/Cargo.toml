[package]
description = "Build-time typed asset codegen for ROSACE apps"
name = "rosace-asset-codegen"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true

# Build-time only: apps depend on this under [build-dependencies] and call
# `rosace_asset_codegen::generate("assets")` from their build.rs. Pure std, no
# deps, so it adds ~nothing to build time.

[lib]
