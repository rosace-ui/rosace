# <Subsystem name>

> One-line description. Which crate(s) this covers.

## In one sentence

The layman version — what this does and why anyone should care, no jargon.

## Mental model

An analogy, or a diagram (mermaid). The goal: after reading this, a newcomer can *predict* how the subsystem behaves without reading the code.

## How it works

The actual flow, step by step. **Link every concrete claim to code** as `[`thing`](../../crate/src/file.rs)` (line numbers optional but nice). Prefer linking over re-explaining code — the code is the source of truth.

## Key types

- [`TypeName`](../../crate/src/file.rs) — one line on what it is.
- …the handful of names a contributor must know. Not an exhaustive list.

## Why it's like this

Link the decisions that shaped it: `See [D0XX](../DECISIONS.md)`. Explain the tradeoff in plain terms.

## Gotchas & invariants

- The rules that must hold (and what breaks if they don't).
- The surprising behaviors that have bitten people before.
