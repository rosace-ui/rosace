# Phase 24 — Real Native Host Projects for Mobile (D106)

> Status: IN PROGRESS (Steps 1-3 landed 2026-07-08; Step 4 partial — Android
> side only)
> Started: 2026-07-08
> Completed: —
> Decision: **D106** — winit cannot own the iOS app; mobile needs a real,
> editable native host project generated by `tzr new`.
>
> Progress:
> - Step 1 ✅ Native-bridge FFI boundary — new crate `tezzera-ffi`
>   (`src/engine.rs`, `src/surface.rs`, `src/event.rs`,
>   `include/tzr_engine.h`). `App::launch`'s ~450-line `run_layered` closure
>   was extracted (behavior-preserving — full workspace test suite +
>   desktop-example smoke-run both green afterward) into
>   `tezzera::FrameEngine` (new `tezzera/src/engine.rs`) so it's drivable
>   from both the existing winit loop AND this new boundary without
>   duplicating the reconciler/paint/input logic. `RawSurface` implements
>   `wgpu::rwh::HasWindowHandle`/`HasDisplayHandle` directly from a raw
>   `CAMetalLayer*`/`ANativeWindow*` pointer — `GpuPresenter::new` needed ZERO
>   changes, it was already raw-window-handle-generic, not winit-specific.
>   `TzrInputEventFfi` is one flat `#[repr(C)]` struct (no new `InputEvent`
>   variants — native touch converts to `MouseDown`/`Move`/`Up`, same as
>   winit's existing `Touch` handling). The 5 `#[no_mangle] extern "C"` `
>   tzr_engine_*` functions themselves are NOT in `tezzera-ffi` — only a
>   concrete app knows its root `Component`, so they're per-app glue (~15
>   lines), demonstrated in `tezzera-ffi/examples/ios_stub.rs` (the template
>   Step 2's `tzr new` codegen will follow).
>   **Verified for real, not just "compiles"**: cross-compiled the example to
>   `aarch64-apple-ios-sim` (zero warnings); `nm -gU` confirmed all 5 symbols
>   exported with correct C names; a hand-written throwaway Swift/UIKit app
>   (scratchpad only, not committed) linked the resulting staticlib, called
>   `tzr_engine_init` against a REAL `CAMetalLayer`-backed `UIView` on a
>   booted iOS 17.4 Simulator, got a non-null handle back, and — beyond the
>   documented exit bar — a `CADisplayLink`-driven `tzr_engine_frame` loop
>   actually rendered and presented a real frame (the theme's background
>   color visibly on screen) for 9+ seconds with no crash before the
>   throwaway app was uninstalled.
> - Step 2 ✅ Real `.xcodeproj` + Swift host — `tzr new` now generates
>   `ios/App.xcodeproj/` (a genuine Xcode project) alongside `ios/App/`
>   (`AppDelegate.swift`, `SceneDelegate.swift`, `EngineViewController.swift`
>   — real code, not winit's implicit AppDelegate) when `ios` is selected.
>   **`.pbxproj` format choice, de-risked before any codegen was written**:
>   used `PBXFileSystemSynchronizedRootGroup` (`objectVersion` 77, Xcode
>   16+) — Xcode auto-discovers `.swift` files added to `App/` with no
>   `PBXFileReference`/`PBXBuildFile` pair needed per file, unlike the
>   legacy pbxproj format. Confirmed real (this machine has Xcode 26.2) by
>   reading an actual Xcode-generated `.pbxproj` shipped in
>   `/Applications/Xcode.app/.../visionOS/Application/App.xctemplate/` for
>   the exact object shapes, then hand-authoring + verifying a minimal
>   single-target spike with real `xcodebuild -list`/`build` in the
>   scratchpad *before* wiring any generator code around it.
>   `GENERATE_INFOPLIST_FILE = YES` (build-setting-driven, no physical
>   `Info.plist` inside the synced folder — avoids a known gotcha where a
>   physical file there collides with Xcode's own Info.plist processing).
>   The physical `ios/Info.plist` this module also generates is unrelated —
>   kept only for the older Phase 20-22 hand-rolled `tzr run --target ios`
>   harness, untouched per the Migration Rule.
>   A `PBXShellScriptBuildPhase` runs `cargo build --lib --target
>   <triple>` (triple picked from Xcode's own `$PLATFORM_NAME`) before
>   Swift compilation, copying the resulting `.a` into `$BUILT_PRODUCTS_DIR`
>   (already on Xcode's default search path — no explicit
>   `LIBRARY_SEARCH_PATHS` needed); `OTHER_LDFLAGS` carries `-l<crate_name>`
>   plus the system frameworks Step 1's stub verification had already
>   determined are needed (UIKit/QuartzCore/Metal/Foundation/CoreGraphics/
>   Security/CoreFoundation).
>   **`tzr_engine_resize` ABI grew 4 params** (`safe_top/right/bottom/left`)
>   — `Engine::resize` now calls `tezzera_core::set_safe_area` before
>   resizing, and `EngineViewController.viewDidLayoutSubviews` feeds it real
>   `UIView.safeAreaInsets`, replacing the Phase 20-22 winit
>   `outer_size()`/`inner_size()`-diffing workaround as this step's doc text
>   called for. `tezzera-ffi/examples/ios_stub.rs` updated to match.
>   `tzr new`'s `Cargo.toml` gains `crate-type = ["cdylib", "staticlib",
>   "rlib"]` + a `tezzera-ffi` path dependency when iOS/Android is selected;
>   a new `src/ffi.rs` (mirroring `ios_stub.rs`'s pattern) wires the app's
>   own `AppRoot` — the SAME root component desktop/web already drive.
>   `icons.rs`'s iOS output moved to `ios/App/Assets.xcassets/
>   AppIcon.appiconset/`, the real asset-catalog location
>   `ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon` wires.
>   **Verified for real, exceeding Step 1's bar**: `tzr new --platforms
>   desktop,ios` scaffolded a real app; `xcodebuild -project
>   ios/App.xcodeproj -scheme App -sdk iphonesimulator build` succeeded
>   (the Cargo build script phase genuinely built and linked the Rust
>   staticlib); installed + launched on a booted iOS 17.4 Simulator — a
>   screenshot shows the REAL app UI (AppBar with title + theme-toggle
>   button, the "Counter" list tile with its subtitle), not just a blank
>   frame, proving the full FrameEngine/paint pipeline renders identically
>   to desktop through the native host. Stayed alive with no crash across
>   the verification window. Touch-tap interaction specifically (beyond
>   render correctness) could not be verified in this sandbox — GUI
>   automation via AppleScript/System Events was blocked by a missing
>   accessibility permission that couldn't be granted non-interactively;
>   noted as a manual follow-up rather than silently skipped. Desktop/web-
>   only scaffolds confirmed byte-for-byte unaffected (diffed against
>   current behavior); full workspace build + all 73 test suites green.
> - Step 3 ✅ Android: real Gradle project + JNI — `tzr new` now generates
>   `android/` (root + `app` Gradle modules, `AndroidManifest.xml`,
>   `MainActivity.kt`) when `android` is selected. Chose plain `Activity` +
>   `SurfaceView`, NOT `GameActivity`/`NativeActivity` — the FFI boundary
>   already drives the engine explicitly via JNI calls from Kotlin
>   (init/resize/touch/frame), so `android-activity`'s native-entrypoint
>   machinery (letting Rust own `android_main`) isn't needed; mirrors why
>   Step 2 uses a thin Swift `AppDelegate` instead of winit's implicit one.
>   Winit is still a transitive dependency (through `tezzera-platform`,
>   for shared types) and hard-errors on `target_os = "android"` without an
>   `android-activity` backend feature picked even though its own event
>   loop is never run on this path — fixed by enabling
>   `winit/android-native-activity` for the android target only.
>   **JNI, not the plain C ABI iOS uses**: Kotlin's `external fun` resolves
>   to a symbol literally named `Java_<package>_<Class>_<method>` (JNI's
>   mangling: `.` → `_`, a literal `_` → `_1`) — computed per-app at
>   codegen time by `jni_class_prefix`/`jni_mangle` in `new.rs` from the
>   bundle id, since (unlike iOS's fixed `tzr_engine_*` names) the symbol
>   is package-specific. New crate dependencies: `jni` (safe `JNIEnv`/
>   `JObject` wrappers) + `ndk-sys` (`ANativeWindow_fromSurface`/`_release`
>   raw bindings) — `jni` is the de facto standard for this, used by
>   `winit`/`wgpu`'s own Android backends, so there wasn't real
>   decision-space to weigh. `tezzera-ffi::AndroidSurfaceHandle` wraps the
>   acquire/release pair in `Drop`, kept alive alongside the `Engine` (both
>   torn down together in `nativeShutdown`) per `RawSurface`'s existing
>   lifetime contract.
>   `app/build.gradle.kts`'s `cargoBuildAndroid` task cross-compiles the
>   Rust `cdylib` and stages it into `src/main/jniLibs/<abi>/` before
>   Gradle's own resource merge — the Android counterpart to Step 2's
>   `PBXShellScriptBuildPhase`. NDK root and linker path are resolved from
>   `ANDROID_NDK_HOME`/`ANDROID_HOME` at build time (not baked in as an
>   absolute path) so the generated project isn't tied to one machine;
>   host-tag detection covers macOS/Linux/Windows x86_64 only (see Known
>   Issues). Used `ProcessBuilder` instead of Gradle's `exec {}` DSL inside
>   the task body — `exec` is a `Project` extension function not reliably
>   resolvable from inside a registered task's `doLast` (confirmed:
>   "Unresolved reference 'exec'" against this machine's Gradle 9.4).
>   A real Gradle wrapper (`gradlew`/`gradlew.bat`/`gradle-wrapper.jar`) is
>   generated by actually running `gradle wrapper` against the scaffolded
>   project at `tzr new` time (soft-fails with a clear note if `gradle`
>   isn't on PATH) — hand-authoring the wrapper jar as a template wasn't
>   an option (it's binary), and a project depending on the user having
>   system Gradle installed forever isn't the right end-state either.
>   Safe-area is a known simplification here: `MainActivity.kt` passes
>   zero insets to `nativeResize` rather than a real `WindowInsets`-driven
>   status-bar/cutout/gesture-nav treatment (iOS's Step 2 equivalent uses
>   real `UIView.safeAreaInsets`) — flagged, not silently claimed
>   equivalent; follow-up work.
>   **Verified for real**: `rustup target add aarch64-linux-android` +
>   `.cargo/config.toml` NDK-clang linker entries let
>   `cargo build -p tezzera-ffi --example android_stub --target
>   aarch64-linux-android --release` produce a genuine ARM64 `.so`;
>   `llvm-nm -D` confirmed all 5 `Java_dev_tezzera_stub_MainActivity_*`
>   symbols exported with the exact names JNI resolution needs. Then, for
>   a real `tzr new --platforms android`-scaffolded app (bundle id
>   `dev.tezzera.android_test`, deliberately chosen to exercise the `_1`
>   JNI-underscore-mangling edge case): `./gradlew assembleDebug` —
>   **BUILD SUCCESSFUL**, 36 tasks executed including the real
>   `cargoBuildAndroid` cross-compile step; the resulting APK genuinely
>   contains `lib/arm64-v8a/libandroid_test.so` (6.1MB, real compiled Rust)
>   and `classes.dex`; `llvm-nm -D` against the `.so` confirmed
>   `Java_dev_tezzera_android_1test_MainActivity_native{Init,Resize,Touch,
>   Frame,Shutdown}` — exactly the mangled name `jni_class_prefix` computed,
>   proving the codegen's symbol-name derivation is correct against a real
>   linker's output, not just unit-tested in isolation. `tzr run --target
>   android` (Step 4, Android side — see below) then re-ran the same
>   project through the actual CLI command and confirmed it drives Gradle
>   correctly (`BUILD SUCCESSFUL`, incremental — 35 of 36 tasks up-to-date).
>   **Not verified**: install + launch on a real emulator/device — no
>   Android system image could be installed on this machine without
>   risking a disk-full failure mid-session (2-3GB free for most of this
>   work; a system image is 1GB+); `adb devices` was empty throughout.
>   `tzr run --target android` handles this honestly (builds successfully,
>   detects no device via `adb devices`, prints a manual
>   `adb install <apk>` instruction) rather than pretending success.
>   Flagged in Known Issues alongside Windows — this Mac genuinely cannot
>   verify on-device rendering for the same category of reason (no
>   available target environment), though Android's story is meaningfully
>   stronger than Windows's here: real NDK cross-compilation AND a real
>   Gradle-built APK are both proven, vs. Windows having neither.
> - Step 4 (Android side only) ✅ `tzr run --target android` drives the
>   real Gradle project (`./gradlew assembleDebug`) instead of a
>   hand-rolled pipeline (there was never a Phase 20-22 Android harness to
>   supersede, unlike iOS), then `adb install`/`adb shell am start` if a
>   device is connected. `preflight_android` checks `android/gradlew`
>   exists (hard-fail with a fix if not) and warns (soft) if `adb` is
>   missing. iOS's Step 4 (replacing the Phase 20-22 `codesign`/`simctl`
>   pipeline with real `xcodebuild`) is NOT done — the existing hand-rolled
>   iOS harness still works and wasn't broken, so this was scoped as
>   lower-priority than getting Android's Step 3 fully verified within
>   this session; remains open.
> Remaining: Step 4 (iOS side — replace the hand-rolled `codesign`/`simctl`
> pipeline with real `xcodebuild`), Step 5 (capability-plumbing proof).

## Why This Phase

Phase 20–22's iOS/Android bring-up proved the rendering engine compiles and
runs on-device (compositor, safe-area, touch input, `tzr run --target ios`).
That was the right first step, but the result — an `Info.plist` next to a raw
executable, no `.xcodeproj` — is not a viable end-state. Verified directly in
winit 0.30.13's source: `EventLoop::run_app` calls `UIApplicationMain` on iOS
and winit generates its own implicit `AppDelegate`. There is no supported way
to run winit inside an `AppDelegate` the user controls. That makes an entire
class of real-app requirements structurally unreachable as long as winit owns
the iOS process: push-notification registration, deep links / universal
links, background tasks, and anything else that lives in AppDelegate/
SceneDelegate methods. Xcode-editable entitlements, capabilities, icons, and
signing are also unreachable without a real `.xcodeproj`.

Android is not as blocked — winit's Android backend runs on `android-activity`,
which explicitly supports subclassing `NativeActivity`/`GameActivity` in
Kotlin — but we still generate no Gradle project, manifest, or Activity today.

## The Model

- **iOS**: a real `.xcodeproj`, generated by `tzr new`, with our OWN thin
  `AppDelegate`/`SceneDelegate` (Swift) — not winit's. The Rust engine
  compiles to a **staticlib** and is linked into the Xcode target. A small C
  FFI boundary (init / resize / input-event / frame-tick) lets the Swift host
  create the `UIView`/`CAMetalLayer` and drive the Rust render loop inside it.
  Winit's iOS backend is retired for the shipped app (desktop/web keep using
  winit — it works well there and isn't blocked the same way).
- **Android**: a real Gradle project — `build.gradle`, `AndroidManifest.xml`,
  a `MainActivity.kt` subclassing `GameActivity`/`NativeActivity`. The Rust
  engine compiles to a **cdylib (.so)**, called via JNI, rendering into a
  `SurfaceView`. Winit can still be used here.
- **`tzr run --target ios|android`** invokes the real toolchains
  (`xcodebuild`, `./gradlew`) against the generated project instead of our
  hand-rolled `codesign`/`simctl` bundling from Phase 20–22 — the generated
  project becomes the source of truth; `tzr` drives it, doesn't replace it.
- This is the "bare" packaging style from D104, done for real; the deferred
  "harness generator" choice in D104 is resolved by this decision.

## Steps

### Step 1 — Design the native-bridge FFI boundary
Define the minimal C ABI the native host (Swift/Kotlin) and the Rust engine
cross: `tzr_engine_init(surface_handle, width, height, scale) -> EngineHandle`,
`tzr_engine_resize(handle, width, height)`, `tzr_engine_input(handle,
InputEventFfi)`, `tzr_engine_frame(handle) -> FrameCommands` (or a direct
render-into-surface call), `tzr_engine_shutdown(handle)`. Keep it small — this
boundary is the seam every future native feature (push token delivery, deep
link URL, permission result) crosses.

Exit: FFI header + Rust `#[no_mangle] extern "C"` surface compiles; a hand-
written throwaway Swift/Kotlin stub can call `init` and get a handle back.

### Step 2 — iOS: thin Swift AppDelegate + Xcode project template
Generate a minimal `.xcodeproj` (AppDelegate.swift, SceneDelegate.swift,
Info.plist, a `UIViewController` owning a `CAMetalLayer`-backed view) that
links the Rust staticlib and drives it through the Step 1 FFI on
`viewDidLoad`/`viewWillLayoutSubviews`/touch callbacks. Reuse the safe-area
math already proven in Phase 20–22 (now driven by real `UIView.safeAreaInsets`
since we own the view hierarchy — no more `outer_size()`/`inner_size()`
workarounds).

Exit: `tzr new app --platforms ios` produces a `.xcodeproj` that opens in
Xcode, builds, and runs the counter example on-device/simulator, with the
same visual correctness already verified in Phase 20–22 (notch/home-indicator
insets, touch input) — via our own AppDelegate instead of winit's.

### Step 3 — Android: Gradle project + MainActivity
Generate `build.gradle`, `AndroidManifest.xml`, `MainActivity.kt` (subclasses
`GameActivity` or `NativeActivity`), loading the Rust cdylib via
`System.loadLibrary` and JNI. Decide whether to keep winit here (subclassing
is supported) or mirror the iOS FFI boundary for consistency — pick based on
how much of Phase 20-22's Android-specific work (once started) can be reused.

Exit: `tzr new app --platforms android` produces a Gradle project that builds
an APK and runs on an emulator/device.

### Step 4 — `tzr run --target ios|android` drives the real toolchains
Replace the Phase 20-22 hand-rolled `codesign`/`simctl install` pipeline with
`xcodebuild -scheme ... -destination ...` (+ `simctl install/launch` only for
the final install/launch step) for iOS, and `./gradlew installDebug` +
`adb shell am start` for Android.

Exit: `tzr run --target ios` / `--target android` work unchanged from the
developer's perspective, but underneath drive a real, user-editable project.

### Step 5 — Permission/capability plumbing proof
Pick one real platform feature unreachable today (e.g. camera permission
prompt, or push-notification registration) and wire it end-to-end: Info.plist
key + Swift AppDelegate code + FFI callback into Rust — proving the native
host model actually delivers what Info.plist-only could not.

Exit: a demo app requests and receives a native permission result in Rust
code, via the new bridge.

**Scope note (added 2026-07-08, raised in discussion, not designed yet)**:
this one-feature proof is the first slice of a broader **platform
capabilities API** that belongs on this same FFI boundary — sensors, camera,
biometrics, other permission prompts, and (per D105) "what platform am I on."
`tezzera-platform` is purely windowing/input today (confirmed in the
`CRATE_CONTRACTS.md` audit — an earlier planning pass imagined a
`Permission`/`Haptics`/`Biometrics`/`use_sensor()` surface for it that was
never built); winit cannot reach any of these on iOS for the same structural
reason it can't own the AppDelegate (D106). Once Step 1's FFI boundary
exists, each capability is just another message over it — no separate
architecture needed. Display insets/orientation build on the already-shipped
`tezzera_core::SafeArea`, now sourced from the real `UIView.safeAreaInsets`
this phase gives us instead of the winit `outer_size()`/`inner_size()`
workarounds. Desktop/web capability access doesn't need this FFI detour and
can be designed independently. Don't over-build this in Step 5 — one proof
is enough to validate the model; a fuller capabilities surface is later work
once real apps show which capabilities are actually needed first.

## Migration Rule

Desktop and web are UNCHANGED — winit continues to own them; this phase is
mobile-only. The Phase 20-22 hand-rolled iOS harness (`Info.plist` +
`simctl`) is superseded, not deleted immediately — keep it working until
Step 2 lands a working `.xcodeproj` replacement, so there's no dead period
with no working iOS path.

## DO NOT

- Do not attempt to keep winit owning the iOS app lifecycle "for now" while
  building the native host in parallel — the whole point is winit cannot
  reach AppDelegate-level features; there is no partial version of this fix.
- Do not skip the FFI boundary design (Step 1) and start writing Swift/Kotlin
  glue ad hoc — every native feature added later crosses this boundary; get
  its shape right first.
- Do not remove the Phase 20-22 safe-area/touch-input logic — its *math* was
  verified correct on-device; only the surface it's plumbed through changes
  (real `UIView` instead of winit's window).
